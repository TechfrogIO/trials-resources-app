// import React from "react";
// import { render, screen } from "@testing-library/react";
// import { EditorPageTest } from "./EditorPage";

describe("EditorPage", () => {
  /**
   * MOCK API METHODS
   */
  // it("renders", () => {
  //   render(<EditorPageTest />);
  //   expect(screen.getAllByText("My Notes App")).toHaveLength(1);
  // });
  it("is true", () => {
    expect(true).toEqual(true);
  });
});
